# M7. pandas 시계열 도구 — 활동지

이름: ____________

M6에서는 시트에서 하던 일을 코드로 재현했다. 오늘은 **시트로는
어려운 것들**이다. Colab 새 노트북에서 시작한다.

## 1. 날짜를 이름표로 — 날짜 인덱스

M6의 불러오기 코드에 `index_col` 하나를 추가한다:

```python
import pandas as pd

url = "https://raw.githubusercontent.com/jaehoonkim/timeseries-academy/main/data/seoul-temp-daily.csv"
df = pd.read_csv(url, parse_dates=["date"], index_col="date")
df.head()
```

`index_col="date"`는 date 열을 각 행의 **이름표(인덱스)** 로 삼는다는
뜻이다. 이제 행을 "1067번째 줄"이 아니라 **날짜로 부를 수 있다**.

**Q1.** `df.head()`의 모양이 M6 때와 어떻게 달라졌는가?

답: ____________

## 2. 날짜로 부르기

M5에서 2026년 7월을 찾으려고 "1067행"을 계산했던 것을 기억하는가?
이제는 이렇게 부른다. 읽는 법:

> `df.loc["2023-08-10"]`
> = 이름표(인덱스)가 "2023-08-10"인 **행을 찾아 달라**
> (`loc` = location. 1절에서 날짜를 행의 이름표로 삼은 이유가 이것)

- `df["temp_avg"]`가 **열**(세로)을 고르는 것이라면, `df.loc[...]`는
  **행**(가로)을 고른다.
- "2026-01"처럼 달까지만 쓰면 그 달의 **모든 행**이 나온다.

```python
df.loc["2023-08-10"]
```

```python
df.loc["2026-01"]
```

```python
df.loc["2026-01"]["temp_avg"].mean()
```

```python
df.loc["2026-01-20":"2026-01-22"]
```

**Q2.** `df.loc["2023-08-10"]`의 temp_avg는 얼마인가?
1부에서 만난 적 있는 날이다 — 무슨 날인가?

답: ____________

**Q3.** 2026년 1월의 평균은 얼마인가? 이 숫자를 M4 어디에서 봤는가?

답: ____________

**Q4.** `df.loc["2026-01-20":"2026-01-22"]`에 나온 사흘은 어떤
날들인가? (temp_avg를 보면 안다)

답: ____________

## 3. resample — 시간 단위를 바꾸는 도구

M4에서 월별 평균을 내려면 세 단계가 필요했다: ① `=MONTH()`로 월 열
만들기 ② 피벗에서 행=월로 묶기 ③ 값을 AVERAGE로 바꾸기.
날짜 인덱스가 있으면 **resample**이라는 도구가 이걸 한 줄로 한다.
먼저 읽는 법부터:

> `df["temp_avg"].resample("ME").mean()`
> = temp_avg를 / **월 단위로 다시 묶어서** / 묶음마다 **평균**

- `resample(...)` — "시간 단위로 다시 묶기". 일별 데이터를 월별·주별·
  연별 묶음으로 바꾼다 (피벗의 "행=월" 역할).
- `"ME"` — 어떤 단위로 묶을지. Month End의 약자로, 월 단위로 묶고
  결과에 월말 날짜 라벨을 붙인다는 뜻.
- `.mean()` — 묶음마다 어떻게 요약할지 (피벗의 "값=AVERAGE" 역할.
  `.max()`로 바꾸면 월별 최고값이 된다).

이제 실행해 보자:

```python
monthly = df["temp_avg"].resample("ME").mean()
monthly
```

```python
monthly.plot()
```

**Q5.** `monthly`에는 값이 몇 개 있는가? M4의 월별 피벗 표(12칸)와
무엇이 다른가?

답: ____________

이번엔 연 단위로 뭉쳐 보자:

```python
df["temp_avg"].resample("YE").mean()
```

**Q6.** 연평균이 13.2 → 12.9 → 12.7 → 12.1로 해마다 내려간다.
"서울이 추워지고 있다"는 뜻일까? 함정이 있다 — 아래 코드로
찾아보자. (힌트: M4에서 반쪽짜리였던 연도들)

```python
df["temp_avg"].resample("YE").count()
```

답: ____________

## 4. rolling의 숨은 옵션 — 지연 없는 이동평균

`rolling(window=7)`은 M6에서 배운 "7일 창을 하루씩 밀며 계산하기"다.
지금까지의 창은 항상 **그날까지의 7일**(뒤로 난 창)이었다 — 그래서
M3에서 이동평균이 급락을 **며칠 늦게** 따라갔다. `center=True`
옵션은 창의 **가운데**에 그날을 놓는다(앞 3일 + 그날 + 뒤 3일).
그러면 지연이 사라진다:

```python
avg = df["temp_avg"]
compare = pd.DataFrame({
    "actual": avg,
    "trailing": avg.rolling(window=7).mean(),
    "centered": avg.rolling(window=7, center=True).mean(),
})
compare.loc["2023-08"].plot()
```

**Q7.** 태풍 골짜기(8/10) 근처에서 trailing과 centered 중 어느 쪽의
골짜기가 실제(actual)와 같은 위치에 있는가?

답: ____________

**Q8.** 이렇게 좋은 centered를 M5의 예측 대결에는 쓸 수 없다.
왜일까? (힌트: 8/10의 centered 값을 계산하려면 어떤 날들이 필요한가)

답: ____________

## 5. 결측치 — 구멍 난 데이터 다루기

우리 데이터는 1,096일 무결점이지만, 실제 데이터에는 구멍(결측)이
흔하다. 시나리오: **태풍에 관측 장비가 사흘 고장 났다면?**

```python
broken = df["temp_avg"].copy()
broken.loc["2023-08-10":"2023-08-12"] = None
broken.isna().sum()
```

`None`은 "값 없음", `isna().sum()`은 구멍의 개수 세기다.

이제 구멍을 채워 보자. 도구는 `interpolate` — 우리말로 **보간**이다:

> `broken.interpolate()`
> = broken의 구멍마다 / **양옆에 남은 값을 직선으로 이어** /
> 구멍 자리의 값을 그 선 위에서 읽어 채우기

그래프로 상상하면: 구멍 양옆에 살아남은 두 점(8/9와 8/13)을 자로
긋고, 사라진 사흘을 그 직선 위의 점으로 채우는 것이다. 그래서
사흘짜리 구멍은 시작 값에서 끝 값까지 똑같은 간격으로 한 걸음씩
내려가는(또는 올라가는) 계단이 된다.

```python
fixed = broken.interpolate()
fixed.loc["2023-08-09":"2023-08-13"]
```

**Q9.** 보간이 채운 세 값은 얼마인가? 실제 값(22.0 / 22.9 / 23.8)과
비교하면 어떤가?

답: ____________

**Q10.** 채우는 대신 구멍 난 사흘을 **지워 버리면**(행 삭제) 시계열에
어떤 문제가 생길까? (힌트: 8/9 다음 행이 8/13이 된다)

답: ____________

## 6. 생각해 보기

**Q11.** 보간된 데이터로 2023년 8월 평균을 내면 25.6°C가 아니라
26.0°C가 나온다. M2 Q11에서 "태풍 날을 지우면 8월이 실제보다 덥게
왜곡된다"고 했던 것과 이어 보자 — 결측을 채울 때 조심해야 할 점은
무엇인가?

답: ____________
