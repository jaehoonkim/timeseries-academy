# M6. 파이썬으로 다시 보기 — 활동지

이름: ____________

1부에서 스프레드시트로 한 일을 **코드**로 다시 해본다. 데이터는
시트가 아니라 CSV 주소에서 직접 불러오므로, 준비물은 구글 계정뿐이다.

## 1. 첫 셀 — 파이썬과 인사

1. [colab.research.google.com](https://colab.research.google.com)에
   접속해 **새 노트북**을 만든다.
2. 화면의 빈 칸이 **셀**이다. 셀에 코드를 쓰고 **Shift+Enter**를
   누르면 실행되고, 아래에 결과가 나온다.
3. 첫 코드를 실행해 보자:

```python
print("안녕, 시계열!")
```

4. 파이썬은 계산기이기도 하다. 새 셀에:

```python
(1 + 2 + 2 + 3 + 42) / 5
```

**Q1.** 결과는 얼마인가? 이 계산을 어디서 본 적이 있는가?
(힌트: M2의 첫 활동)

답: ____________

## 2. 데이터 불러오기 — 1,096일을 세 줄로

새 셀에 아래를 입력하고 실행한다. (따옴표·괄호까지 정확히!)

```python
import pandas as pd

url = "https://raw.githubusercontent.com/jaehoonkim/timeseries-academy/main/data/seoul-temp-daily.csv"
df = pd.read_csv(url, parse_dates=["date"])
df
```

한 줄씩 뜻을 붙이면:

| 코드 | 하는 일 | 시트에서는 |
|---|---|---|
| `import pandas as pd` | 표 전용 도구 상자를 꺼내 `pd`라고 부르기 | (시트를 여는 것) |
| `pd.read_csv(url, ...)` | 주소에서 CSV 읽어 오기 | `=IMPORTDATA(...)` — **같은 주소다!** |
| `parse_dates=["date"]` | date 열을 날짜로 인식시키기 | 서식 → 숫자 → 날짜 (M1의 45139 사건) |
| `df = ...` | 읽어 온 표를 `df`라는 이름에 담기 | (시트 문서 그 자체) |

이어서 새 셀에 하나씩:

```python
len(df)
```

```python
df.tail()
```

**Q2.** `len(df)`의 결과는? 그 숫자의 의미는 무엇인가?

답: ____________

**Q3.** `df.tail()`(끝부분 5줄)의 마지막 날짜는 언제인가?
1부에서 쓰던 것과 같은 데이터라고 확인할 수 있는가?

답: ____________

## 3. M1 재현 — 그래프 한 줄

M1에서 범위 선택 → 삽입 → 차트 → 꺾은선으로 했던 일:

```python
df.plot(x="date", y="temp_avg")
```

**Q4.** 무슨 모양이 보이는가? M1에서 그린 3년 그래프와 같은가?

답: ____________

M1 추가 실습의 "선 3개짜리 그래프"도 한 줄이다:

```python
df.plot(x="date", y=["temp_avg", "temp_max", "temp_min"])
```

## 4. M2 재현 — 평균과 중앙값

새 셀에 하나씩 실행한다:

```python
df["temp_avg"].mean()
```

```python
df["temp_avg"].median()
```

```python
df["temp_avg"].max()
```

```python
df["temp_avg"].min()
```

**Q5.** 네 값은 각각 얼마인가? max와 min이 서울 기온으로 상식적인
범위인지도 확인하자 (M2의 오타 탐지를 코드로 한 셈이다).

mean: ______ median: ______ max: ______ min: ______

7월(마지막 31일)만 잘라서 M2의 값과 맞춰 보자:

```python
df.tail(31)["temp_avg"].mean()
```

```python
df.tail(31)["temp_avg"].median()
```

**Q6.** M2에서 구했던 평균 26.1, 중앙값 25.8과 일치하는가?

답: ____________

## 5. M3 재현 — 이동평균

M3에서 `=AVERAGE(B2:B8)`을 채우기로 1,090행 복사했던 일이 한 줄이
된다. 먼저 읽는 법:

> `df["temp_avg"].rolling(7).mean()`
> = temp_avg에 / **7일짜리 창을 하루씩 밀며**(rolling) / 창마다 **평균**

- `rolling(7)` — M3에서 손으로 밀던 바로 그 **창**이다. rolling은
  "굴러간다"는 뜻 — 창이 하루씩 굴러 내려간다.
- `.mean()` — 창마다 평균을 낸다. 그 결과가 이동평균.
- `df["ma7"] = ...` — 계산 결과를 `ma7`이라는 **새 열**에 담는다.
  M3에서 E열에 이동평균을 만들던 것과 같은 일이다.

```python
df["ma7"] = df["temp_avg"].rolling(7).mean()
df.head(10)
```

**Q7.** 표 앞부분의 `ma7` 열에 **NaN**이라는 값이 6개 보인다.
NaN은 무엇일까? M3의 시트에서 본 것 중 무엇과 같은가?

답: ____________

365일 이동평균까지 얹어 M3의 마지막 그래프를 재현하자:

```python
df["ma365"] = df["temp_avg"].rolling(365).mean()
df.plot(x="date", y=["temp_avg", "ma7", "ma365"])
```

**Q8.** M3에서 발견했던 것 — 계절 물결이 사라진 자리의 완만한
오르막 — 이 다시 보이는가?

답: ____________

## 6. 생각해 보기

**Q9.** 시트에서 몇십 분 걸렸던 M1~M3의 작업이 코드로는 몇 줄이
됐나? 코드로 하면 좋은 점을 두 가지 적어 보자.

답: ____________

**Q10.** 반대로, 코드보다 시트가 더 나은 경우는 언제일까?
도구를 고르는 나만의 기준을 한 문장으로 써 보자.

답: ____________
