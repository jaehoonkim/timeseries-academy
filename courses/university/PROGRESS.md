# 강의자 학습 진행 — 대학생 과정

모듈마다: practice 수행 → 질문 해소 → 체크.

- [x] 기반 — 둘째 데이터 수집·검증 (2026-08-27): 서울 지하철 일별
  승하차(data/seoul-subway-daily.csv, 기온과 같은 1,096일 구간,
  결측 0). 시차 7 자기상관 0.68 > 시차 1의 0.35 — 설계가 노린
  "달력의 세계" 성격 실측 확인. 재현 스크립트
  data/build-subway-daily.py.
- [x] U1 파이프라인 압축 재구축 (2026-08-28)
- [x] U2 분해 정식 — STL (2026-08-30)
- [x] U3 시계열의 어휘 — ACF·PACF와 정상성 (2026-08-30)
- [ ] U4 지수평활 가족 — ETS
- [ ] U5 ARIMA
- [ ] U6 SARIMA와 예측 구간
- [ ] U7 특징 기반 ML 도전
- [ ] U8 파이널 프로젝트

## 질문 기록

막혔던 지점과 답을 여기 남긴다. 가치 있는 것은 해당 모듈 teacher.md
"예상 질문"으로 옮긴다.

- **U1** Q8 "가장 아픈 요일"이 안 읽힘 — '아프다'는 선수 은유를
  고등 과정에서 가져왔는데, 대학은 독립 과정이라 첫 등장인데도
  병기가 없었음(H6 '미스'·H7 '벌었는지' 건과 같은 유형) — 저작
  원칙(압축 은유는 첫 등장에 용어 병기)이 과정 경계에서 리셋된다는
  교훈 → "가장 아픈 요일 — 오차가 가장 큰 요일 —"로 병기.
  → worksheet 반영됨.
- **U2** 2절 "계절을 걷어낸다 = 그 달의 보통을 뺀다"의 의미가
  선언만 있고 다리가 없음 — 빼기가 왜 계절 제거인지, 남은 편차가
  무엇을 말하는지 구체 사례 부재(H4는 다리가 있었으나 과정
  독립이라 U에선 첫 등장, 패턴 16 유형) → dev 셀 뒤에 "잣대의
  교체" 문단 추가(-12.3도 → 1월치고 -9.1도, 한여름 27도 → +0.3).
  → worksheet 반영됨.
- **U2** 2절 `trend_hand.min().round(2)`가 Colab에서 에러 —
  실측: Colab(pandas 2.2.3)의 `min()`은 맨 파이썬 float를
  반환(`type()` 확인), `mean()` 계열은 .round 가능 타입이라 같은
  버릇의 U1 코드는 통과했던 것. 로컬은 pandas 2.2.3/3.0.5 어느
  조합으로도 재현 불가 — Colab 환경 특이로 결론. 교훈: 스칼라
  반올림은 내장 `round(x, n)`만 쓸 것(로컬 실행 검증이 Colab을
  보증하지 않는다, 잣대 패턴 17) → U1 네 줄·U2 세 줄 교체,
  Series의 `.round()`는 유지, 고등·중등은 패턴 자체 없음 확인.
  → worksheet(U1·U2) 반영됨.
- **U2** 3절 "Loess = 매끈한 곡선을 맞추는 방법"에서 강의자가
  Loess의 정체를 물음 — teacher 심화의 세 줄 요약("국소 가중
  회귀")이 설명 재료로 얇았음 → rolling 평균과의 비교
  다리(평균=수평선 vs 작은 회귀선, 균등 가중 vs 거리 가중)와
  "손 분해 두 부품을 Loess로 바꾸고 반복시킨 정식판 = Q4 잔차
  반감의 이유"까지 심화 보강. → teacher 반영됨.
- **U2** 3절 `stl_t.plot();`의 세미콜론이 설명 없이 첫 등장(패턴
  2) — 빼고 실행하면 그림이 두 번 나와 "잘못했나" 싶은 지점
  (STL의 .plot()만 Figure를 돌려줘 결과값 표시로 한 번 더
  그려짐; pandas .plot()은 Axes라 중복 없음) → 읽는 법 박스에
  "세미콜론 = 결과값 출력 생략, 빼면 두 번(고장 아님)" 병기.
  → worksheet 반영됨.
- **U2** 5절 '심문'이 표준 용어인지 질문 — 통계 용어가 아니라
  과정의 은유인데 병기 없이 절 제목까지 차지(패턴 16 유형).
  U5에 정식 용어 "잔차 진단"이 예정돼 있어 다리 필수 → 5절 첫
  문장에 "정식 이름은 잔차 진단(residual diagnostics), U5에서
  재회" 병기. → worksheet 반영됨.
- **U3** 1절 ACF 도입에서 '시차'가 정의 없이 일반화됨 — U1에서는
  괄호 병기("시차 1 (어제)")로 통했지만 U3은 "모든 시차, 0부터
  400까지"처럼 이 낱말이 기둥인데 정의 문장이 없었음(패턴 1)
  → 1절 첫머리에 정식 정의(시차 = 두 시점 사이의 간격, 시차 0 =
  자기 자신이라 닮음 1) 추가. → worksheet 반영됨.
- **U4** 도입의 '지수평활'이 이름부터 던져짐 — 한자어 둘(지수·
  평활)이 겹친 용어인데 풀이 없이 등장(원칙 5 위반, H7 '회귀'
  건과 같은 유형) → 도입에 이름 풀이 추가: 평활 = 매끄럽게
  고르기(U2 rolling의 그 일), 지수 = 무게가 0.7배씩 옅어지는
  모양(α=0.3 구체 수열), rolling과의 차이는 무게 배분뿐.
  → worksheet 반영됨.
- **U4** '배합'이 표준 용어인지 질문 — 일상어(요리의 그 배합)를
  빌린 과정의 은유인데 정식 용어 병기가 없었고, 첫 등장은 사실
  U3 Q9였음(패턴 16 유형) → U4 1절에 "정식 용어로는 가중
  평균(무게 합 1인 평균), SES = 두 재료를 α:(1−α)로 섞는 가중
  평균" 병기, U3 Q9 첫 등장에도 괄호 풀이 추가.
  → worksheet(U3·U4) 반영됨.
- **U4** 1절 "훈련 2년이 스스로 고른 최선의 배합"이 안 읽힘 —
  훈련 구간(앞 2년)의 압축 + 데이터 의인화가 두 겹으로 겹쳐
  주어가 뭉개짐(H6 '미스' 건 유형) → "fit이 훈련 구간(앞 2년)의
  데이터만 보고 찾아낸 최선의 배합 — 사람이 아니라 데이터가 고른
  답이 naive"로 풀어 씀. → worksheet 반영됨.
