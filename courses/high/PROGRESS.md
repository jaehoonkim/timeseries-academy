# 강의자 학습 진행 — 고등학생 과정

모듈마다: practice 수행 → 질문 해소 → 체크.

- [x] H1 시계열과 파이썬 (2026-08-18)
- [x] H2 분포로 보는 시계열 (2026-08-18)
- [x] H3 평활 — 이동평균과 지수평활 (2026-08-19)
- [x] H4 계절성과 분해 (2026-08-24)
- [x] H5 자기상관 (2026-08-25)
- [x] H6 예측과 평가 (2026-08-25)
- [ ] H7 회귀로 예측 — slides.md가 저작 때 선작성됨(루틴 위반).
  초안으로 두고, 완주 후 실측·질문 기록을 반영해 재검토할 것.
- [ ] H8 미니 프로젝트

## 질문 기록

막혔던 지점과 답을 여기 남긴다. 가치 있는 것은 해당 모듈 teacher.md
"예상 질문"으로 옮긴다.

- **H1** `df.loc["2025-08-01"]`의 `loc`가 뭘 하는 건지 설명 없이
  등장 → "읽는 법" 블록(이름표로 행 찾기, 열 선택과 대비, 부분 날짜)
  추가. 중등 M7 첫 등장 지점에도 동일 반영. → worksheet 반영됨.
- **H2** 히스토그램(bins=30)의 답 "봉우리 2개"가 실제 그림과 안
  맞아 보임 — 가운데 12°C, 15°C 부근의 잔봉우리 때문. bins 나누기의
  우연임을 답에 명시하고, "평균 근처는 골짜기"라는 과장도 "두
  봉우리보다 낮다"로 정정. → worksheet/teacher/answers 반영됨.
- **H2** z-점수 식과 "표준편차 몇 개만큼"이라는 문장이 바로 연결이
  안 됨 → 도시 B 손 계산(빼기→나누기 두 단계) + "거리 ÷ 보폭 = 걸음
  수" 비유를 코드 앞에 추가. → worksheet/teacher 반영됨.
- **H2** (제작 교훈) 한 문단에 물결표(~) 두 개가 있으면 마크다운
  취소선으로 렌더링돼 답이 깨져 보임 → 범위 표기는 문단당 1개까지만.
- **H3** `ma365.plot()`을 "따로 그려서 자세히 보자"는 이유가 설명
  없이 등장 → 겹친 그림은 세로축이 원본 40도 폭에 맞춰져 이 선이
  평평해 보이고, 단독으로 그리면 축이 좁아져 오르막이 드러난다.
  "같은 선도 축 눈금에 따라 다르게 읽힌다"를 worksheet 도입문과
  teacher 예상 질문에 추가. → worksheet/teacher 반영됨.
- **H3** 겹쳐 그린 그래프에서 어느 선이 어느 것인지 산문의 색 순서
  안내("파랑→주황→초록")에 의존 → 겹쳐 그리기 셀 6개 전부에
  `label=`·`legend=True`를 명시해 그래프가 범례로 직접 답하게 함.
  → worksheet/teacher 반영됨.
- **H3** Colab에 한글 글꼴이 없어 범례 한글이 □로 깨짐 →
  worksheet 2절 첫머리에 `!pip install -q koreanize-matplotlib` +
  `import koreanize_matplotlib` 준비 셀 추가, teacher 예상 질문에
  재실행 안내. → worksheet/teacher 반영됨.
- **H3** 지수평활 도입(5절)의 무게 나열(0.3, 0.21, 0.147, …)만으로는
  이해가 안 됨 → 점화식 읽기 "오늘 평활값 = 0.3×오늘 + 0.7×어제
  평활값(새 소식 30% + 기억 70%)"을 워크시트에 추가하고, teacher
  0:52 흐름에 "무게 나열이 낯설면 점화식부터"를 명시.
  → worksheet/teacher 반영됨.
- **H4** 3절 도입의 "그 달의 보통"이 무슨 말인지 안 읽힘 — 1절
  분식집의 "보통"과 2절 프로필을 잇는 다리 없이 표현만 등장 →
  도입문에 정의("그 날짜가 속한 달의 프로필 값, 8월 10일이면 8월
  값 26.7°C")와 분식집 비교(금요일의 보통 73명)를 추가하고,
  teacher 0:22 흐름에 "transform 전에 이 정의부터"를 명시.
  → worksheet/teacher 반영됨.
- **H4** 6절에서 '보통'을 '평균'이라고 봐도 되는지 헷갈림 —
  "평균은 '보통'을 잡는 여러 방법 중 하나"라는 방향이 안 보임 →
  6절 끝에 "평균으로 잡은 것도 선택" 문단(치우친 데이터에선
  중앙값이 '보통'에 더 가깝다), teacher 예상 질문에 "왜 하필
  평균이에요?" 추가. → worksheet/teacher 반영됨.
- **H4** 2절 Q4·Q5를 그래프에서 읽는 건지 헷갈림 → 숫자는 첫
  셀이 출력하는 12줄 표에서 읽는 게 의도. 특히 Q5(7월 26.4 vs
  8월 26.7)는 그래프로는 구분이 안 되는 게 문제의 장치 — "직감
  확인은 그림이 아니라 숫자 목록으로". 자료 변경 없음.
- **H4** 3절 도입 "1,096일 각각을 저마다의 '보통'과 비교"가 안
  읽힘 — 예시가 프로필 값에서 멈추고 편차 계산까지 안 이어짐 →
  가상의 두 날로 계산을 완결(+3.4 조금 더운 날 / -6.8 한파)하고
  "공통 기준이 아니라 날마다 자기 달의 기준"을 명시. 예시 날짜는
  카눈 날짜(8/10)와 겹치지 않게 "8월의 어느 날"로.
  → worksheet 반영됨.
- **H4** 2절 mean()과 3절 transform("mean")의 "달마다 평균"이 왜
  방법이 다른지 헷갈림 — 같은 계산의 출력 모양 차이임이 안 보임 →
  "왜 12줄이면 안 되나" 문단(뺄셈은 같은 날짜 자리끼리 짝을
  짓는다)과 profile/monthly 대조 그림을 transform 읽는 법 아래
  추가. → worksheet 반영됨.
- **H5** 4절 "계절의 착시"가 갑자기 등장 — 제목의 0.98이 본문에
  없고(Q5 빈칸이라 출처 불명), 편차 a가 2절 것인지 안 읽히고,
  대각선이 왜 계절 그 자체인지 다리가 없음 → 도입에 Q5 출처·a
  되짚기·그림 독법(오른쪽 위 = 여름날의 짝, 왼쪽 아래 = 겨울날의
  짝, 대각선 = 계절의 배치)을 추가하고, 편차 lag plot 뒤에 "모든
  날이 자기 달의 보통을 0으로 삼아 포개진다" 문장을 놓음.
  → worksheet 반영됨.
- **H5** corr가 읽는 법 없이 코드에 첫 등장 — 같은 모듈의
  shift·autocorr는 박스가 있는데 정작 핵심 도구만 없었음 → corr
  읽는 법 박스 추가(correlation 줄임말, 1절 r과 연결).
  → worksheet 반영됨.
- **H5** (제작 교훈) 설명을 보강하다 문단이 벽이 됨 → 한 문단 한
  역할로 분리하고 병렬 관찰은 불릿으로. 워크시트에 없는
  비유("어제의 실력")를 쓰지 말고 절의 핵심 문구("어제가 오늘을
  말해 주는가")로 통일.
- **H5** ACF 정의가 괄호 각주 문단에 묻히고 "꺼지는 속도 =
  기억"이 선언으로만 있음 → 정의를 독립 문단으로(새 계산이 아니라
  시차별 반복, function인 이유), 독법을 별도 문단으로(각 점 =
  "k일 전을 알면 오늘을 얼마나 말할 수 있나", 0 = 잊음).
  → worksheet 반영됨.
- **H5** Q8 힌트 `acf_a.head()`는 기본 5행(시차 5, 0.14)까지라
  정답인 시차 6(0.098)이 출력 범위 밖 → `head(10)`으로 정정.
  → worksheet 반영됨.
- **H5** shift(182)가 왜 182인지 설명 없이 등장 → "반년 전(365일의
  절반, 시차 182)"로 산수를 드러냄. → worksheet 반영됨.
- **H5** Q11 "약 6배"의 출처가 안 보임 → Q10 두 값의 나눗셈(9.5 ÷
  1.6 ≈ 6)과 평균 전략이 빗나가는 계절별 크기(여름 14도·겨울
  18도)를 답에 추가. → teacher/answers 반영됨.
- **H5** 원본 lag plot 온도 범위가 파일마다 제각각(영하 15도 /
  영하 10도) → 실측(-12.3~32.8°C)에 맞춰 "영하 12도~33도"로 통일.
  → worksheet/teacher/answers 반영됨.
- **H6** teacher의 압축 표현 "RMSE는 큰 미스에 가중"이 무슨 말인지
  안 읽힘 — '미스'는 심판·선수 은유가 용어 자리까지 침범한 말이고,
  압축하면서 목적어("가중치를")까지 빠짐 → 모듈 전체에서 '미스'를
  '오차'로 통일하고 "RMSE는 큰 오차에 가중치를 준다"는 완전한
  문장으로 정리. (제작 교훈: 은유는 서사에만, 개념 자리는 정확한
  용어로.) → worksheet/teacher/practice 반영됨.
- **H7** 시작 전에 '회귀'라는 이름부터 막힘 — 이름이 하는 일을
  설명해 주지 않는 한자어라 문턱이 됨 → 하는 일("재료들의 가장
  좋은 배합을 데이터에서 찾기", H3의 배합 비율을 사람 대신
  컴퓨터가 정하는 것)과 이름 유래(갈턴의 "극단은 평균으로
  되돌아온다", 역사적 유물)를 분리해 설명. worksheet에는 둘 다
  이미 있으나(도입의 배합 정의, 4절 갈턴) 질문은 읽기 전에
  나온다 — 도입에서 미리 짚도록 teacher 예상 질문 첫머리에 추가.
  → teacher 반영됨.
- **H7** (후속) 예상 질문 한 항목으로는 회귀 개념 설명 자료가
  부족 — teacher가 실행(대본·정답)만 있고 개념의 전체 그림이
  없었음 → teacher에 "회귀란 무엇인가 — 강의자용 개관" 절 추가
  (한 줄 정의, 배합=계수, 갈턴 유래, 통계·ML의 공통 뿌리이자
  첫 지도학습, 과정 안의 위치). → teacher 반영됨.
- **H7** 4절 coef_ 출력이 `array([...])`로 대괄호에 싸여 나오는
  이유 설명 없음 — 절편은 맨숫자인데 기울기만 목록인 비대칭이
  "고장인가?" 싶은 지점(잣대 패턴 1) → "재료를 표로 받았으니
  계수도 재료마다 하나씩 목록으로" 문단 추가, 5절 두 칸 복선
  포함. → worksheet 반영됨.
- **H7** 4절이 fit 직후 coef_·intercept_를 찍게 하면서 "왜 성적보다
  이 숫자부터 보나"를 말해 주지 않음 — 읽기 연습(Q2·Q4·Q7·Q11)은
  네 번인데 이유 선언이 없음(패턴 7) → "성적은 얼마나, 계수·절편은
  어떻게" 문단 + 읽는 법 불릿(기울기 = 재료를 얼마나 믿나, 절편 =
  심어 놓은 밑값, 1절 사례 소환) 추가. → worksheet 반영됨.
- **H7** 5절 도입 "어제 한 칸에 든 정보는 naive가 이미 다 쓰고
  있다"가 안 읽힘 — '한 칸'(특징 테이블의 어제 열)과 '다 쓴다'
  (그 숫자를 그대로 예측으로 냄)는 은유가 풀이 없이 압축돼 있고,
  Q5의 답을 선언만 하는 위치 문제도 겹침(패턴 7, H6 '미스' 건과
  같은 유형) → 1/20의 재료 -4.7 하나라는 구체 사례로 풀어 쓰고
  "같은 재료로는 같은 답"을 결론 문장으로 세움. → worksheet
  반영됨.
- **H7** 5절 프로필 출력이 12줄인 게 안 읽힘 — "2025-07-31"이
  달력의 앞 7개월 자르기로 연상되는데, 실은 시작이 2023년 8월이라
  훈련 2년에 열두 달이 모두 들어 있음. H6 완주자도 출력을 마주한
  순간 걸림 → 프로필 셀 직후에 "시작이 8월, 열두 달 두 번씩"
  문단 + 검산(달마다 약 60일 × 12 ≈ Q3의 730일, 패턴 9) 추가.
  → worksheet 반영됨.
- **H7** Q7을 coef_ 출력에서 어떻게 읽는지 연결 고리 없음 —
  출력 `array([0.797, 0.203])`에 이름표가 없어 어느 칸이 어느
  재료인지 학생이 추측해야 함(패턴 8: 힌트가 답을 못 만듦) →
  fit 셀 직후에 "칸의 순서는 fit에 준 열 순서 그대로(첫 칸 어제,
  둘째 칸 달의 보통)" 문단 추가. → worksheet 반영됨.
