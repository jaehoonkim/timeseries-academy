# 강의자 학습 진행 — 고등학생 과정

모듈마다: practice 수행 → 질문 해소 → 체크.

- [x] H1 시계열과 파이썬 (2026-08-18)
- [x] H2 분포로 보는 시계열 (2026-08-18)
- [x] H3 평활 — 이동평균과 지수평활 (2026-08-19)
- [x] H4 계절성과 분해 (2026-08-24)
- [ ] H5 자기상관
- [ ] H6 예측과 평가
- [ ] H7 회귀로 예측
- [ ] H8 미니 프로젝트

## 질문 기록

막혔던 지점과 답을 여기 남긴다. 가치 있는 것은 해당 모듈 teacher.md
"예상 질문"으로 옮긴다.

- **H1** `df.loc["2025-08-01"]`의 `loc`가 뭘 하는 건지 설명 없이
  등장 → "읽는 법" 블록(이름표로 행 찾기, 열 선택과 대비, 부분 날짜)
  추가. 중등 M7 첫 등장 지점에도 동일 반영. → worksheet 반영됨.
- **H2** 히스토그램(bins=30)의 답 "봉우리 2개"가 실제 그림과 안
  맞아 보임 — 가운데 12°C, 15°C 부근의 잔봉우리 때문. bins 나누기의
  우연임을 답에 명시하고, "평균 근처는 골짜기"라는 과장도 "두
  봉우리보다 낮다"로 정정. → worksheet/teacher/answers 반영됨.
- **H2** z-점수 식과 "표준편차 몇 개만큼"이라는 문장이 바로 연결이
  안 됨 → 도시 B 손 계산(빼기→나누기 두 단계) + "거리 ÷ 보폭 = 걸음
  수" 비유를 코드 앞에 추가. → worksheet/teacher 반영됨.
- **H2** (제작 교훈) 한 문단에 물결표(~) 두 개가 있으면 마크다운
  취소선으로 렌더링돼 답이 깨져 보임 → 범위 표기는 문단당 1개까지만.
- **H3** `ma365.plot()`을 "따로 그려서 자세히 보자"는 이유가 설명
  없이 등장 → 겹친 그림은 세로축이 원본 40도 폭에 맞춰져 이 선이
  평평해 보이고, 단독으로 그리면 축이 좁아져 오르막이 드러난다.
  "같은 선도 축 눈금에 따라 다르게 읽힌다"를 worksheet 도입문과
  teacher 예상 질문에 추가. → worksheet/teacher 반영됨.
- **H3** 겹쳐 그린 그래프에서 어느 선이 어느 것인지 산문의 색 순서
  안내("파랑→주황→초록")에 의존 → 겹쳐 그리기 셀 6개 전부에
  `label=`·`legend=True`를 명시해 그래프가 범례로 직접 답하게 함.
  → worksheet/teacher 반영됨.
- **H3** Colab에 한글 글꼴이 없어 범례 한글이 □로 깨짐 →
  worksheet 2절 첫머리에 `!pip install -q koreanize-matplotlib` +
  `import koreanize_matplotlib` 준비 셀 추가, teacher 예상 질문에
  재실행 안내. → worksheet/teacher 반영됨.
- **H3** 지수평활 도입(5절)의 무게 나열(0.3, 0.21, 0.147, …)만으로는
  이해가 안 됨 → 점화식 읽기 "오늘 평활값 = 0.3×오늘 + 0.7×어제
  평활값(새 소식 30% + 기억 70%)"을 워크시트에 추가하고, teacher
  0:52 흐름에 "무게 나열이 낯설면 점화식부터"를 명시.
  → worksheet/teacher 반영됨.
- **H4** 3절 도입의 "그 달의 보통"이 무슨 말인지 안 읽힘 — 1절
  분식집의 "보통"과 2절 프로필을 잇는 다리 없이 표현만 등장 →
  도입문에 정의("그 날짜가 속한 달의 프로필 값, 8월 10일이면 8월
  값 26.7°C")와 분식집 비교(금요일의 보통 73명)를 추가하고,
  teacher 0:22 흐름에 "transform 전에 이 정의부터"를 명시.
  → worksheet/teacher 반영됨.
- **H4** 6절에서 '보통'을 '평균'이라고 봐도 되는지 헷갈림 —
  "평균은 '보통'을 잡는 여러 방법 중 하나"라는 방향이 안 보임 →
  6절 끝에 "평균으로 잡은 것도 선택" 문단(치우친 데이터에선
  중앙값이 '보통'에 더 가깝다), teacher 예상 질문에 "왜 하필
  평균이에요?" 추가. → worksheet/teacher 반영됨.
- **H4** 2절 Q4·Q5를 그래프에서 읽는 건지 헷갈림 → 숫자는 첫
  셀이 출력하는 12줄 표에서 읽는 게 의도. 특히 Q5(7월 26.4 vs
  8월 26.7)는 그래프로는 구분이 안 되는 게 문제의 장치 — "직감
  확인은 그림이 아니라 숫자 목록으로". 자료 변경 없음.
- **H4** 3절 도입 "1,096일 각각을 저마다의 '보통'과 비교"가 안
  읽힘 — 예시가 프로필 값에서 멈추고 편차 계산까지 안 이어짐 →
  가상의 두 날로 계산을 완결(+3.4 조금 더운 날 / -6.8 한파)하고
  "공통 기준이 아니라 날마다 자기 달의 기준"을 명시. 예시 날짜는
  카눈 날짜(8/10)와 겹치지 않게 "8월의 어느 날"로.
  → worksheet 반영됨.
- **H4** 2절 mean()과 3절 transform("mean")의 "달마다 평균"이 왜
  방법이 다른지 헷갈림 — 같은 계산의 출력 모양 차이임이 안 보임 →
  "왜 12줄이면 안 되나" 문단(뺄셈은 같은 날짜 자리끼리 짝을
  짓는다)과 profile/monthly 대조 그림을 transform 읽는 법 아래
  추가. → worksheet 반영됨.
