# H6. 예측과 평가 — 활동지

이름: ____________

H5 마지막에 두 전략을 어림으로 겨뤄 봤다 — "내일은 오늘과
같다"는 naive가 3년 평균을 말하는 전략보다 6배 정확했다. 그리고
다음 시간에 이 대결을 정식으로 치르자고 했다. 오늘이 그 시간이다.

정식 경기에는 두 가지가 필요하다 — 미래를 미리 보지 않는다는
**규칙**(홀드아웃)과, 몇 도나 빗나갔는지 재는 **심판**(MAE·RMSE).
이 둘이 서면 선수도 늘릴 수 있다: H3에서 그래프를 매끈하게 펴
주던 이동평균과 지수평활이 예측 선수로 입장한다. 매끈한 선은
예측도 잘할까? Colab 새 노트북에서 시작한다.

## 1. 손으로 워밍업 — 한파 주간을 naive로 채점하기

H5의 2026년 1월 한파 주간, 이번엔 편차가 아니라 **실제 기온**이다
(H5 표의 편차에 1월의 보통 -3.2도를 도로 더한 값). (단위: °C)

| 날짜 | 1/18 | 1/19 | 1/20 | 1/21 | 1/22 | 1/23 | 1/24 | 1/25 |
|---|---|---|---|---|---|---|---|---|
| 기온 | -1.0 | -4.7 | -10.4 | -11.0 | -10.6 | -8.0 | -7.3 | -8.2 |

naive는 매일 "내일은 오늘과 같다"고 말한다. 1/19에 대한 예측은
1/18의 -1.0, 오차는 실제 - 예측 = -4.7 - (-1.0) = **-3.7**이다.

**Q1.** 7일의 예측과 오차를 채우자. 첫 칸은 채워 두었다:

| 날짜 | 1/19 | 1/20 | 1/21 | 1/22 | 1/23 | 1/24 | 1/25 |
|---|---|---|---|---|---|---|---|
| 예측(=전날 기온) | -1.0 | | | | | | |
| 오차(실제-예측) | -3.7 | | | | | | |

**Q2.** 심판 1호 — 오차의 절댓값 7개를 더해 7로 나누자. 이 값을
**MAE**(mean absolute error, 평균 절대 오차)라 부른다: "이 선수는
평균적으로 몇 도 빗나가는가".

절댓값의 합: ______  MAE: ______ °C

**Q3.** 심판 2호 — 오차 7개를 각각 **제곱**해 평균 내고, 다시
제곱근을 씌우자(2.8² = 7.84가 힌트). 이 값이 **RMSE**(root mean
squared error)다.

제곱의 합: ______  제곱의 평균: ______  RMSE: ______ °C

**Q4.** RMSE가 MAE보다 크게 나왔다. 일곱 오차 중 **어느 이틀**이
이 차이를 만들었나? 그 이틀은 한파의 어떤 국면인가?

답: ____________

제곱은 큰 수를 훨씬 크게 만든다 — 0.4²은 0.16으로 줄지만 5.7²은
32.49로 커진다. 그래서 MAE는 모든 미스를 공평하게 재고, **RMSE는
큰 미스에 가중치를 준다**. 심판이 둘인 이유다: 조금씩 자주
틀리는 선수와 가끔 크게 틀리는 선수를 구분해 준다.

## 2. 준비 — 데이터

준비(한글 글꼴)와 데이터 로드는 지금까지와 같다:

```python
!pip install -q koreanize-matplotlib
import koreanize_matplotlib
```

```python
import pandas as pd

url = "https://raw.githubusercontent.com/jaehoonkim/timeseries-academy/main/data/seoul-temp-daily.csv"
df = pd.read_csv(url, parse_dates=["date"], index_col="date")
t = df["temp_avg"]
```

## 3. 규칙 — 미래를 미리 보지 않는다

시험 문제를 미리 본 학생의 점수는 실력이 아니다. 예측도 같다 —
전략을 세울 때 쓰는 데이터와 채점에 쓰는 데이터를 **시간 순서로**
갈라 둔다. 이 약속을 **홀드아웃**(holdout)이라 부른다. 읽는 법:

> `t[:"2025-07-31"]`
> = 처음부터 2025-07-31**까지** — 앞 2년, **훈련**(train) 구간
> `t["2025-08-01":]`
> = 2025-08-01부터 끝**까지** — 마지막 1년, **시험**(test) 구간
> (콜론이 "부터/까지"다 — H1의 날짜로 묻기가 범위로 확장된 것)

```python
train = t[:"2025-07-31"]
test = t["2025-08-01":]
len(train), len(test)
```

**Q5.** 훈련과 시험은 각각 며칠인가? 왜 무작위로 섞어 자르지 않고
시간 순서로 자를까?

훈련: ______ 일  시험: ______ 일

이유: ____________

## 4. 심판 — MAE와 RMSE를 코드로

1절의 손 계산을 코드로 옮긴다. 오차 시리즈 e가 있을 때 — 읽는 법:

> `e.abs().mean()`
> = 오차의 **절댓값**(abs)들의 평균 — **MAE**
> `(e**2).mean() ** 0.5`
> = 오차를 제곱해 평균 내고 다시 제곱근 — **RMSE**
> (`** 0.5`가 제곱근이다 — 0.5제곱 = √. 루트를 씌우는 이유는
> 단위를 °C²에서 °C로 되돌리기 위해서다)

첫 선수 **평균**으로 심판을 시운전하자. 이 선수는 시험 기간 내내
훈련 구간의 평균 하나만 말한다:

```python
pred = pd.Series(train.mean(), index=test.index)
e = test - pred
e.abs().mean(), (e**2).mean() ** 0.5
```

**Q6.** 평균 선수의 성적은? (다음 절의 순위표 첫 줄에 적자.)
train.mean()은 몇 도인가?

train.mean(): ______ °C

## 5. 선수 입장 — 예측 만들기

나머지 선수들의 예측은 전부 한 줄씩이다. 공통 규칙이 하나 있다:
**내일의 예측에는 오늘까지의 정보만** 쓸 수 있다. 코드에서 이
서약이 `.shift(1)`이다 — H5에서 배운 그 도구가, 여기서는 "예측을
하루 뒤로 밀어 어제까지의 정보로 만들기"로 다시 쓰인다.

```python
pred_naive = t.shift(1)[test.index]
pred_snaive = t.shift(365)[test.index]
pred_ma = t.rolling(window=7).mean().shift(1)[test.index]
pred_ses = t.ewm(alpha=0.3).mean().shift(1)[test.index]
```

- **naive**: 내일은 오늘과 같다 — H5의 그 선수.
- **계절 naive**(seasonal naive): 내일은 **작년 같은 날**과 같다
  — shift(365). 우리 시험 구간에서는 정확히 1년 전 날짜가 된다.
- **이동평균**: 내일은 **어제까지 7일의 평균**과 같다 — H3의
  rolling.
- **지수평활**: 내일은 어제까지의 지수평활값과 같다 — H3의 ewm,
  새 소식 30%(alpha=0.3).

**Q7.** 이동평균과 지수평활에는 왜 `.shift(1)`이 붙어 있을까?
H3에서 배운 rolling의 성질(창에 오늘이 포함된다)을 떠올려 보자.

답: ____________

## 6. 대결 — 순위표

각 선수를 4절과 같은 방법으로 채점한다. 예:

```python
e = test - pred_naive
e.abs().mean(), (e**2).mean() ** 0.5
```

**Q8.** pred를 바꿔 가며 순위표를 채우자 (소수 첫째 자리까지):

| 선수 | MAE (°C) | RMSE (°C) |
|---|---|---|
| 평균 | | |
| naive | | |
| 계절 naive | | |
| 이동평균(7일) | | |
| 지수평활(0.3) | | |

**Q9.** 우승자는 누구인가? 그 선수의 MAE를 H5에서 본 어림값과
비교해 보자. 그리고 우승의 이유를 H5에서 배운 말(자기상관)로 한
문장으로 쓰자.

우승: ____________  이유: ____________

## 7. 결과 읽기 — 진 선수들에게 배운다

**Q10.** 이동평균과 지수평활은 H3에서 그래프를 그토록 매끈하게
만들던 도구인데 naive에게 졌다. H3에서 배운 이 도구들의 성질
(창이 클수록, 기억이 길수록 어떻게 되었나)로 패인을 설명해 보자.

답: ____________

**Q11.** 계절 naive의 **최악의 날**을 찾아가 보자 — H1의 idxmax를
오차에 쓴다:

```python
e_snaive = test - pred_snaive
e_snaive.abs().idxmax(), e_snaive.abs().max()
```

그날의 실제 기온과 작년 같은 날의 기온을 `t.loc[...]`로 꺼내
비교하자. 계절 naive는 무엇을 몰라서 이렇게 크게 틀렸나?

최악의 날: ____________  실제: ______ °C  작년 같은 날: ______ °C

답: ____________

**Q12.** 같은 방법으로 naive의 최악의 날을 찾자. 전날과 당일의
기온은 각각 몇 도인가? 이런 날을 H5 마지막 문제에서 뭐라고
불렀나?

최악의 날: ____________  전날: ______ °C  당일: ______ °C

이런 날의 이름: ____________

## 8. 생각해 보기

**Q13.** 지수평활의 새 소식 비율 alpha를 0.3에서 점점 올려 1에
가깝게 하면, 이 선수의 예측은 어떤 선수를 닮아 가는가? "매끈한
그래프"와 "정확한 예측"은 같은 것인가?

답: ____________

**Q14.** H5 Q12의 분식집(하루 손님 수, 요일 리듬)이라면 계절
naive는 shift를 며칠로 두는 게 좋을까? 그 이유는?

shift( ______ )  이유: ____________

**Q15.** 어떤 학생이 alpha를 0.1부터 0.9까지 다 돌려 보고 **시험
구간 성적이 가장 좋은 값**을 골랐다. 이것이 반칙인 이유를 3절의
규칙으로 설명해 보자. 그럼 alpha는 어디서 골라야 할까?

답: ____________

다음 시간(H7)에는 드디어 naive에 도전한다 — 월·요일·어제
기온(lag)을 **특징**으로 만들어 회귀에 먹이고, 오늘 세운 심판
앞에서 순위표를 갱신할 수 있는지 본다. 오늘 naive가 크게 틀린
날(전환점)이 공략 지점이다.
