# H1. 시계열과 파이썬 — 활동지

이름: ____________

## 1. 시계열이란 — 순서가 정보다

두 데이터를 비교해 보자.

- (가) 우리 반 30명의 **키** 목록
- (나) 내 키를 **3년 동안 매달 잰** 기록

**Q1.** (가)와 (나) 중 순서를 섞어도 아무 문제가 없는 쪽은? 반대로
순서를 섞는 순간 의미가 망가지는 쪽은? 그 이유를 한 문장으로 쓰자.

답: ____________

숫자를 **시간 순서대로** 기록한 데이터를 **시계열(time series)**
데이터라고 한다. 핵심은 순서가 정보라는 것 — (나)를 섞으면 "자라는
중인가?"라는 질문에 답할 수 없게 된다. 오늘부터 이 시계열을 파이썬으로
다룬다.

## 2. Colab 첫 셀

1. [colab.research.google.com](https://colab.research.google.com)
   접속 → **새 노트북**.
2. 빈 칸(**셀**)에 코드를 쓰고 **Shift+Enter**로 실행한다. 이 조작이
   오늘 수업의 전부다.

```python
print("시계열 시작")
```

```python
(24.8 + 25.2 + 24.3) / 3
```

## 3. 3년치 데이터를 세 줄로

서울의 하루 평균·최고·최저 기온 3년치를 불러온다. 코드를 실행하기
전에 **읽는 법**부터:

> `pd.read_csv(url, parse_dates=["date"], index_col="date")`
> = url의 CSV를 읽되 / date 열을 **날짜로 인식**시키고 /
> 그 날짜를 각 행의 **이름표(인덱스)** 로 삼는다

- `import pandas as pd` — 표 데이터 전용 도구 상자를 꺼내 `pd`라는
  별명으로 부른다.
- `parse_dates=["date"]` — 컴퓨터는 "2023-08-01"을 그냥 글자로
  읽는다. 이 옵션이 그것을 진짜 날짜로 바꾼다.
- `index_col="date"` — 날짜를 행의 이름표로. 잠시 뒤 "행 번호" 대신
  **날짜로 데이터를 부르기** 위한 준비다.

```python
import pandas as pd

url = "https://raw.githubusercontent.com/jaehoonkim/timeseries-academy/main/data/seoul-temp-daily.csv"
df = pd.read_csv(url, parse_dates=["date"], index_col="date")
df.head()
```

새 셀에 하나씩:

```python
len(df)
```

```python
df.tail()
```

**Q2.** `len(df)`의 결과는? 이 숫자는 무엇을 세는 것인가?

답: ____________

**Q3.** 데이터의 시작일과 마지막 날은? (head와 tail에서 읽는다)

답: ____________ ~ ____________

## 4. 그래프 한 줄

1,096개의 숫자를 훑는 대신 그린다:

```python
df["temp_avg"].plot()
```

**Q4.** 어떤 모양이 보이는가? 이 모양이 왜 생기는지 한 문장으로
추측해 보자. (이 모양의 정체는 H4에서 정식으로 다룬다)

답: ____________

세 열을 한 번에 겹쳐 그릴 수도 있다:

```python
df.plot()
```

**Q5.** 세 선(최고·평균·최저)의 **간격**을 관찰하자. 간격이 넓은
계절과 좁은 계절이 있는가?

답: ____________

## 5. 날짜로 묻기

날짜가 인덱스이므로, 데이터에 날짜로 질문할 수 있다. 읽는 법:

> `df.loc["2025-08-01"]`
> = 이름표(인덱스)가 "2025-08-01"인 **행을 찾아 달라**
> (`loc` = location. 3장에서 날짜를 행의 이름표로 삼은 이유가 이것)

- `df["temp_avg"]`가 **열**(세로)을 고르는 것이라면, `df.loc[...]`는
  **행**(가로)을 고른다.
- "2026-01"처럼 달까지만 쓰면 그 달의 **모든 행**이 나온다.

```python
df.loc["2025-08-01"]
```

```python
df.loc["2026-01"]
```

```python
df.loc["2026-01"]["temp_avg"].mean()
```

가장 더운 날을 찾을 때는 `idxmax` — 읽는 법:

> `df["temp_avg"].idxmax()`
> = temp_avg가 **최대인 행의 이름표(= 날짜)** 를 알려 달라
> (`max()`가 "얼마나"라면 `idxmax()`는 "**언제**")

```python
df["temp_avg"].max()
```

```python
df["temp_avg"].idxmax()
```

**Q6.** 3년 중 하루 평균기온이 가장 높았던 날은 언제, 몇 °C인가?

답: ____________ , ______ °C

**Q7.** 같은 방법(`min`, `idxmin`)으로 가장 추웠던 날도 찾자.

답: ____________ , ______ °C

**Q8.** 2026년 1월의 평균기온은 몇 °C인가? 이 달의 그래프
(`df.loc["2026-01"]["temp_avg"].plot()`)에서 유난히 추운 구간을
찾아보자 — 언제인가?

월평균: ______ °C  추운 구간: ____________

## 6. 생각해 보기

**Q9.** 내 주변에서 시계열이 될 수 있는 것을 3가지 적고, 하나를 골라
그래프 모양을 상상해서 말로 설명해 보자. (오르막? 물결? 들쭉날쭉?)

1. ____________ 2. ____________ 3. ____________

고른 것의 상상 그래프: ____________

**Q10.** 이 3년 그래프를 근거로 "내년 8월 1일의 기온"을 짐작할 수
있을까? 있다면 몇 °C쯤이고, 그 근거는 무엇인가? 확신은 어느 정도인가?

답: ____________
